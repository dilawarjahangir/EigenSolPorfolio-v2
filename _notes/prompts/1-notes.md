Pre-req:
- Current dir has a NextJS app

Current task:
- Checkout this project.
- I want you to write me a simple markdown that has pages and sections breakdown in a nested list.
- A table that has libraries used (tailwind, gsap, framer motion, etc.) with ones that are most used in this app's designing at top
- Breakdown notes of components, i.e., either they are page based or ui components (like re-usable atom components, etc.). make a table for counts. and then nested list for short desc.

Write these at `_notes/sequential/1-system/` in few md files. file contents must be concnise and well formatted. e.g., in nested list, you may include bullet `- ` and then a bold title `**Title:** ` followed by one short sentence.

I guess you have got the idea. start now and produce mds in this dir.
let me know when done.

---