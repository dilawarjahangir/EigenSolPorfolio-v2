Pre-req:
- There is a 3d model that has different nodes that we can control
- Using this 3d model, there is a design that is already created at spline.
- Spline makes it heavy for site to load, on the other hand, ThreeJS has better and lighter solutions.
- We can even use Blender to create useful and lighter exports.
- Nextbot 3d model is available at `_notes/sequential/3-nextbot-3d-model/`:
    - We shall study this model and use with ThreeJS
- 3d model's nodes, lighting setup, camera setup, everything is available on spline design that we shall study and place in a markdown file.

Current Task:
- There is an already openned tab `https://app.spline.design/file/1cb4e4ac-19c5-43b1-a4f6-c9fc4fa0cead` in browser
- On left, there is Scene1, that has camera, light, bot model and logo model
- Our current task involves extracting out all important structures
- you can manually click one (and click on a caret on left) to open details.
- These details will open up in a panel on right side, that can be used to scroll, click different tabs, open dropdowns, etc. to find out exact gemoetry nodes and configurations of different types of lights.
- and we shall write them in study notes at `/Users/macintosh/Documents/repo/DilawarJahangir/EigenSolPorfolio-v2/_notes/sequential/4-nexux-robot-hero-scene`:
    - you can write a compact `README.md`
    - and other details in a dir in MDs for human readable compact short manner (in `scene/`)
    - as well as a `threejs/` for details in formats that will later be easy for ThreeJS
    - also a `specs.md` that has for scene specs, cameras specs, logo specs, model specs, etc. that helps us realize how difficult things are (file size, load, etc.). this should alos a compact file.

Toolkit:
- in `_notes/sequential/5-3d-study-env`, you may create temporary scripts for studying 3d mdoel using threejs (later)
- blender is also installed on this system, so we may use python based controlling or blender cli commands (also later approach. not requierd in this task).
- just letting you know that these are tools that we have access to.

For now, only check details and prepare a short plan in chat here.
so that we get ready for our task. and then we shall continue.

---

looks like a great plan. continue. let me know when everything completes.

---